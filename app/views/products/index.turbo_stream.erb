<%= turbo_stream.update "products" do %>
  <% if @products.present? %>
    <div class="grid md:grid-cols-3 grid-cols-2 gap-6">
      <%= render @products %>
    </div>
    <%== pagy_nav(@pagy) %>
  <% else %>
    <p class="text-center text-primary font-bold text-lg tracking-wider leading-loose capitalize">No Product found</p>
  <% end %>
<% end %>